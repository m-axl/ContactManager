# Diário de Desenvolvimento — 2026-06-11

## Trabalho realizado
- Revisão da documentação principal para refletir o estado atual do código.
- Confirmação de que o CLI atual possui opções para adicionar contato, listar contatos e sair salvando os dados.
- Confirmação de que `src/storage.c` salva e carrega contatos usando `data/contacts.txt`.
- Registro de busca e remoção como funcionalidades planejadas, pois ainda não estão implementadas no menu nem em `src/contact.c`.
- Atualização de README, requisitos, arquitetura, casos de uso, testes, backlog, modelo de dados, resultados e UML.

## Decisões de arquitetura
- Confirmado o uso de persistência local via arquivo em `data/contacts.txt` para a primeira fase, mantendo a separação de responsabilidades entre CLI, lógica de contatos e persistência.
- Preservado o padrão modular do projeto: `include/contact.h`, `include/storage.h`, `src/contact.c`, `src/storage.c` e `src/main.c`.
- Definido que a primeira versão de estabilidade deve concluir busca por nome/telefone e remoção antes de avançar para integrações externas.

## Observações
- A documentação agora diferencia funcionalidades implementadas de funcionalidades planejadas.
- Próximo passo: implementar `findContact` e `removeContact`, atualizar o menu e registrar novos testes funcionais.
